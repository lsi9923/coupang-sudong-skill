---
name: coupang-sudong-skill
description: 쿠팡 윙(Coupang Wing) 상위노출 수동 상품등록 전문 스킬. "쿠팡 등록해줘", "쿠팡 등록", "쿠팡 상품등록", "쿠팡 윙 등록", "쿠팡으로 등록" 등의 요청 시 1688/타오바오/도매 소싱 상품 URL을 분석하여 11대 섹션 48개 필드 전수(카테고리 동적 속성, 35개 고시군 매핑, 60~90자 롱테일 상품명, 검색어 태그 20개 풀장착, 해외구매대행 AGENT_BUY 및 PCC 통관 설정, 바코드 없음 사유, 옵션 카르테시안 곱)를 100% 빈틈없이 채워 완성형 마크다운 폼과 CoupangWingListingPayload JSON을 출력한다.
---

# 쿠팡 윙(Wing) 상위노출 수동등록 마스터 프로토콜

네이버 스마트스토어와 쿠팡은 **알고리즘 철학과 입력 폼 구조가 완전히 다른 세계**입니다.  
네이버의 25~30자 제한, 태그 10개 규칙을 쿠팡에 적용하면 노출 기회의 80%를 잃습니다.  
본 스킬은 쿠팡의 노출 알고리즘과 11대 섹션 48개 입력 필드를 **단 하나도 빠짐없이 꽉 채워서** 완성형으로 출력합니다.

---

## 0. 쿠팡 상위노출 5대 핵심 원칙 (네이버와의 차이점)

1. **상품명 100자 롱테일 고밀도 조합 (네이버 25~30자 제한 해제)**:
   - 네이버는 35자 초과 시 간소화 감점이지만, 쿠팡은 **최대 100자까지 수용**합니다.
   - [브랜드] + [핵심 상품명] + [소재/기능] + [용도/타겟] + [동의어 롱테일]을 결합하여 **60~90자 고밀도 상품명**으로 조합합니다.
2. **검색어 20개 풀장착 (네이버 10개 사전 제한 해제)**:
   - 쿠팡은 사전 등재 여부와 무관하게 **최대 20개(각 20자 이내)**의 검색어를 전부 노출 인덱스로 흡수합니다. 빈칸 없이 20개를 꽉 채웁니다.
3. **카테고리별 동적 검색필터 속성(Attributes) 100% 매핑**:
   - 모바일 앱 좌측/상단 필터 검색(기모, C타입, 블랙, 방수 등)에 걸리려면 카테고리 필수/권장 속성이 반드시 채워져 있어야 합니다.
4. **상품정보제공고시 35개 군 정밀 매칭**:
   - 카테고리별 고시 항목 이름이 1글자만 달라도 400 에러로 차단됩니다. 소싱 데이터 스펙을 매핑하고 미확인 항목은 공식 허용 문구인 `'상세페이지 참조'`로 완벽 방어합니다.
5. **해외구매대행(`AGENT_BUY`) 안전 규칙 엄수**:
   - `pccNeeded: true` (개인통관고유부호 필수 수집), `emptyBarcode: true` (바코드 없음 사유: "해외구매대행 상품으로 바코드가 없습니다"), 출고소요일 **7~14일 설정** (국내 1~2일 설정 시 배송지연 패널티 방지).

---

## 1. 4단계 자동화 실행 파이프라인

사용자가 상품 URL 또는 스펙을 주면 질문하지 않고 다음 4단계를 즉시 자동 수행합니다:

### 1단계: 소싱 상품 스펙 전수 분해 (`sourcing-analyzer`)
- 공급가(CNY), 원단/소재, 옵션(색상/사이즈), 썸네일/상세이미지, 무게/치수, 셀링포인트 전수 추출
- 핵심 품목 키워드 도출

### 2단계: 쿠팡 카테고리 & 동적 메타데이터 매핑 (`coupang-category-engine`)
- 노출 카테고리 코드(`displayCategoryCode`) 확정
- 해당 카테고리가 요구하는 필수 속성(Attributes) 및 35개 고시군(Notices) 세부 항목 도출

### 3단계: 48개 필드 무결점 완성형 폼 조립 (`coupang-wing-composer`)
- 60~90자 롱테일 상품명 조합
- 20개 검색어 태그 빈칸 0개 생성
- 판매가(마진 35% 계산), 할인전 정상가, 재고(999), 배송비/반품비 세팅
- 옵션 카르테시안 곱 (최대 200개)

### 4단계: 11대 섹션 폼 마크다운 & JSON 동시 출력
- 복붙용 마크다운 폼 출력
- 크롬 확장 프로그램 Auto-fill 브릿지용 `CoupangWingListingPayload` JSON 블록 출력

---

## 2. 판매자 기본 고정정보 (기본 셋팅)

- **출고지:** 인천광역시 검단구 완정로 146 (리더스빌) 2층 208-43c호 (23466)
- **반품/교환지:** 위와 동일
- **택배사:** CJ대한통운 (`CJGLS`) / 한진택배 (`HANJIN`)
- **과세여부:** 과세 (`TAX`)
- **배송방법:** 해외구매대행 (`AGENT_BUY`)
- **출고소요일:** 7일 (해외 배송 안정권)
- **기본배송비:** 3,500원 ~ 5,000원
- **초도반품비 / 반품배송비:** 3,500원 / 7,000원 (합계 상한 규정 준수)
- **A/S 안내:** 판매자 고객센터 및 1:1 문의

---

## 3. 쿠팡 윙 11대 섹션 출력 양식

1. **노출 카테고리 (필수):** 카테고리명 및 숫자 코드
2. **등록상품명 (필수):** 60~90자 롱테일 상품명 (글자 수 표기)
3. **판매정보/옵션 (필수):** 정상가, 판매가(쿠팡가), 재고, 바코드 없음 사유
4. **상품 이미지 (필수):** 1000×1000 대표 1장 + 추가/상세 최대 9장
5. **상세설명 (필수):** 모바일 최적화 스토리보드 본문 HTML
6. **상품 주요정보 (필수):** 브랜드(노브랜드), 제조사(협력업체), 원산지(중국), 인증유형
7. **상품정보제공고시 (필수):** 카테고리별 품목군 및 N개 세부 항목 전수
8. **배송정보 (필수):** 구매대행 AGENT_BUY, PCC 필수, 출고소요일 7일, 배송비
9. **반품/교환 (필수):** 반품배송비, 초도반품비, 주소지
10. **검색어 태그 (필수):** **정확히 20개 꽉 채움**
11. **검색필터 속성 (필수):** 카테고리 맞춤형 N개 속성 키-값 쌍
